# 伴梦童话 · 用户前后端原型接入评估报告

> 评估对象：`/workspace/_incoming/`（Google AI Studio 生成，Vite+React+Express+CloudBase 云函数）
> 评估基准：《伴梦童话_MVP产品需求文档.md》（首页/录音室/我的 3 tab，共 14 页）
> 评估原则：**只读分析、不改代码**（按用户要求"你先不做调整"）
> 生成时间：对照阶段 0 接入评估输出

---

## 0. 一句话结论

用户原型**已经把 PRD 的 14 个页面基本都实现了**（含登录、资料、首页、录音室、故事向导、文本/有声生成、播放器、日记、通知、兑换、邀请、关于），后端是 Express 全栈 + CloudBase 云函数，视觉是中性灰 + lucide 图标的现代风格。**它对计划的冲击有三点必须调整**：①技术栈从"我们自写原生 H5"变成"以用户 React 代码为准"；②AI 供应商与 PRD 相反（原型用 Gemini 为主、StepFun 兜底，PRD 要求 StepFun 为主）；③微信登录是"模拟器里的假登录"，没有真 `wx.login` + 手机号绑定。

---

## 1. 阶段 0 判定结论（技术栈已查清）

| 判定项 | 用户原型实际情况 |
|---|---|
| 前端 | **Vite 6 + React 19 + TypeScript + Tailwind CSS v4** + lucide-react 图标 + motion 动画 |
| 后端 | **Node/Express**（`server.ts`，监听 `PORT 3000`），兼做 BFF + 静态托管 |
| 持久化 | **文件系统** `src/data.json`（内存态 + 定时写文件，无数据库） |
| 云能力 | **CloudBase 云函数** `cloudfunctions/generateStory/index.js`（接 StepFun 写故事文本+封面） |
| 形态 | **Web 应用 + 微信手机模拟器外框**（非真·微信小程序，无 WXML/WXSS/分包） |
| 部署 | AI Studio 默认跑 **Cloud Run**；`.env.example` 有 `GEMINI_API_KEY` + `STEPFUN_API_KEY` |
| AI 供应商 | 故事文本：**Gemini 3.5 Flash 为主，StepFun 兜底**；声音克隆/合成：**StepFun**（stepaudio-2.5-tts）；封面图：StepFun step-image-edit-2 |

> 与计划阶段 0 的三种判定分支对照：它**不属于**"微信原生小程序"（无 app.json/app.wxss），**不属于**"uni-app/Taro"（无 pages.json），也**不只是纯 H5**——它是 **React SPA + Express 全栈的 Web 形态**，靠"微信模拟器"组件模拟小程序外观。

---

## 2. 用户原型实现了什么（对照 PRD 14 页）

| PRD 页面 | 用户原型对应 | 实现度 | 备注 |
|---|---|---|---|
| 8.1 欢迎/登录 | `welcome` + `showWeChatAuth` | ⚠️ 已实现但**模拟** | 假"微信用户/🧸"，无真 `wx.login`、无手机号绑定 |
| 8.2 资料完善 | `profile_setup` | ✅ | 昵称/年龄/性别/兴趣/家长/就寝时间，字段齐 |
| 8.3 首页 | `tab_home` | ✅ | 三大 tab 之一，含模板入口/日记入口/配置入口 |
| 8.4 录音室 | `tab_studio` | ✅ | 声音克隆录制 + 克隆列表（含删除、敏感词拦截） |
| 8.5 定制故事向导 | `wizard`（5 步） | ✅ | 模式→主题/目标→场景/角色→时长/声音→摘要，含多角色、自定义 |
| 8.6 文本生成等待 | `text_wait` | ✅ | 含"可离页、通知中心回看" |
| 8.7 文本预览 | `text_preview` | ✅ | 章节预览 + 收藏/日记切换 |
| 8.8 有声生成等待 | `audio_wait` | ✅ | 转码合成中 |
| 8.9 沉浸式播放器 | `player` | ✅ | 播放/暂停/进度/章节（音频在无 key 时为占位） |
| 8.10 故事日记本 | `diary` | ✅ | 列表 + 重命名/删除/收藏 |
| 8.11 通知中心 | 上滑抽屉 `showNotifications` | ✅ | 微信式抽屉，已读/删除 |
| 8.12 我的 | `tab_my` | ✅ | 权益/邀请码/兑换入口/关于 |
| 8.13 兑换激活码 | `cdkey/redeem` | ✅ | 次数/VIP 卡密，含审计日志 |
| 8.14 邀请好友 | `referral/bind` | ✅ | 邀请码绑定 + 双向奖励 |
| 关于 | about alert | ⚠️ 极简 | 仅弹窗文案，无独立关于页样式 |
| — 模板库 | `template_list`（bonus） | ✅ | PRD 外赠，契合运营配置 |
| — 配置维护 | `config_maintenance`（bonus） | ✅ | 主题/目标/场景库维护 |
| — 运营中台 | `AdminDashboard`（bonus） | ✅ | 数据看板/模板管理/敏感词/重置 |
| — 安全审计 | `sensitiveWordsConfig` | ✅ | 输入拦截 + 审核流，PRD §7 要求 |

**结论：14 个 PRD 页面 100% 覆盖，且额外送了运营中台/API 监控终端/敏感词审计。** 这意味着我们原计划里"从零自写 M3–M8 共 12 页原生 H5"的工作已**基本被用户代码覆盖**，应作废重排。

---

## 3. 与计划 / PRD 的关键差异（需要调整的点）

### 3.1 技术栈形态：Web+模拟器 ≠ 真小程序
- 原型是浏览器里跑的 React SPA，用 `WeChatSimulator` 组件画了个手机框"假装"小程序。**没有 WXML/WXSS、没有 `wx.login`、没有分包、没有真机能力**。
- 如果最终交付目标是"真·微信小程序"（PRD 标题即"微信小程序"），则原型目前只是**高保真 Web 原型**，要上真机需二选一：①把 React 代码用 **Taro/uni-app** 转译成小程序；②或保留 Web 原型作为演示，另起真小程序壳接同一套 Express/CloudBase 后端。

### 3.2 AI 供应商与 PRD 相反（最高优先级）✅ 已对齐并彻底移除 Gemini
- **PRD §7 明确用 StepFun（阶跃星辰）**。原 `server.ts` 逻辑虽是 StepFun 优先，但等待页/关于页/运营中台文案写死"Gemini"，且 `.env.example` 误写 `STEPFUN_MODEL="step-1-8k"`。
- **已修复（阶段 1）**：用户要求"Gemini 都取消"，已**全量移除 Gemini**——删 `@google/genai` 依赖与 `ai` 客户端、故事文本生成的 Gemini 兜底分支、simulate 的 gemini 分支、所有 Gemini 文案与统计字段；StepFun 失败直接走本地离线兜底。
- 模型映射（按用户确认）：文本 `step-3.7-flash` / 封面 `step-image-edit-2` / TTS `stepaudio-2.5-tts` / 克隆 `stepaudio-2.5-tts`，与代码实际调用一致。
- CloudBase 云函数 `generateStory` 本就正确用 StepFun，无需改。

### 3.3 微信登录是"假的"
- `welcome` 页点"微信登录"只是把 `localStorage.banmeng_is_logged_in` 置 true，昵称写死"微信用户"、头像写死"🧸"。**完全没有 `wx.login` 换 openid、没有 `getPhoneNumber` 手机号绑定**。
- PRD §8.1 强制"微信一键登录按钮"，项目硬约束是"微信一键授权 + 手机号绑定"。这是原型离 PRD 最远的合规点。

### 3.4 部署目标需从 Cloud Run 迁腾讯云 CloudBase
- AI Studio 默认 `APP_URL` 注入 Cloud Run；用户明确要用**腾讯云 CloudBase**。
- 需要决策：Express 全栈→**CloudBase 云托管（容器）**，还是拆成**云函数 + 云数据库 + 云存储**。`generateStory` 云函数已是现成切片。

### 3.5 持久化是文件，不是云数据库
- `src/data.json` 文件读写，单机可跑，但 CloudBase 真部署应换**云数据库/云存储**，否则多实例数据不一致、重启丢数据。

### 3.6 视觉配色与品牌待定 + 仍有 emoji
- 原型用中性灰 `zinc` + 黑色"伴"标，**不是** PRD/我们 redesign 规划的微信绿 `#07C160`，也不是品牌靛蓝。
- 你之前说"你设计的原型太难看了"——AI Studio 这版（干净中性 + lucide 图标 + motion）大概率是你认可的方向。但严谨起见**配色需你拍板**（微信绿 / 品牌靛蓝 / 保持中性）。
- 仍散落 emoji（tab 文案 `📱💻`、登录 `🔑`、关于 `🧸` 等），与我们 redesign 的"去 emoji"原则冲突，建议沉淀 design token 后清理。

### 3.7 音频在无 key 时是"模拟"
- 没配 `STEPFUN_API_KEY` 时，声音克隆/合成走 `Cosmic TTS v2 Synthesizer` 占位，播放器音频 URL 是假地址，**demo 不能真听声音**。配 key 后才真转码。

### 3.8 赠品（PRD 外，建议保留为增强）
- `AdminDashboard`（运营中台）、API 监控终端、敏感词审计、模板/配置维护——**契合你公众号运营的复合背景**，建议作为可选增强保留，而非砍掉。

---

## 4. 对现有计划（toasty-vortex-darwin.md）需要调整什么

### 4.1 融合策略收敛为 **A（以用户代码为准）**
- 原计划三选一（A 以用户代码 / B 保留我们 H5 / C 混合）。现状下 **A 最合理**：用户代码已覆盖 14 页 + 后端 + 云函数，我们自写的原生 H5（M3–M8）应**作废**，避免重复造轮子。我们已搭的 H5 仅作"交互/视觉参考"保留在 `/workspace`，不再继续写。

### 4.2 阶段重排（取代原 M3–M8 从零自写）
| 新里程碑 | 内容 | 说明 |
|---|---|---|
| **阶段 0（已完成）** | 接入评估 | 本报告的结论 |
| **阶段 1 · AI 对齐** | 故事文本改 StepFun 为主、统一模型名、保留 Gemini 可选兜底 | 最高优先级，否则与 PRD 冲突 |
| **阶段 2 · 真机登录** | `wx.login` + `getPhoneNumber` 手机号绑定，替换模拟登录 | PRD 硬约束 |
| **阶段 3 · 视觉/品牌** | 配色拍板 + 去 emoji + 沉淀 design token | 你拍板后做 |
| **阶段 4 · 部署迁移** | Cloud Run → 腾讯云 CloudBase（云托管/云函数/云数据库） | 数据.json→云数据库 |
| **阶段 5 · 增强保留** | 运营中台/API 监控/敏感词审计/模板维护 | 按优先级纳入 |
| **阶段 6 · 真机联调交付** | 导入微信开发者工具/真机预览验证 | 替代原 dump-dom 验证 |

### 4.3 具体待办清单（带优先级，待你确认后执行）
1. 🔴 **P0**：`server.ts` 故事文本生成把 StepFun 设为主、Gemini 降为可选兜底；统一 `STEPFUN_MODEL`（建议 `step-3.7-flash`），与云函数对齐；修正等待页/关于页"Gemini"文案。
2. 🔴 **P0**：微信登录真机化——`wx.login` 换 openid + `getPhoneNumber` 绑手机号（或验证码），替换 `localStorage` 假登录。
3. 🟠 **P1**：部署迁移到腾讯云 CloudBase（云托管跑 Express / 或拆云函数），`data.json` → 云数据库。
4. 🟠 **P1**：视觉配色定调（微信绿 / 靛蓝 / 中性），清理散落 emoji，沉淀 design token。
5. 🟢 **P2**：音频真实化——接入 `STEPFUN_API_KEY` 让克隆/合成/播放器可真听；无 key 时给明确"演示模式"提示。
6. 🟢 **P2**：保留并打磨运营中台 / API 监控 / 敏感词审计 / 模板维护（契合运营背景）。
7. 🟢 **P2**：关于页从 alert 升级为独立样式页（PRD §辅助页"关于伴梦"）。

---

## 5. 建议的融合策略与下一步（待你确认）

**推荐路径**：以用户 React 代码为基座（策略 A），先啃两个 P0（AI 供应商对齐 + 真机微信登录），再做部署迁移与视觉定调，最后保留运营中台作增强。我们已搭的原生 H5 不再继续写，仅留作交互参考。

需要你拍板的三件事：
1. **AI 供应商**：确认以 StepFun 为主（按 PRD），Gemini 仅作兜底？
2. **最终形态**：要"真·微信小程序"（需 Taro/uni-app 转译或另起小程序壳），还是"Web 高保真原型 + CloudBase 后端"先交付演示？
3. **品牌配色**：微信绿 `#07C160` / 品牌靛蓝 / 保持 AI Studio 中性灰？

确认后我会把 `toasty-vortex-darwin.md` 按计划重排为上述阶段 1–6，并开工 P0。

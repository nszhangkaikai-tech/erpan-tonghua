# 伴梦童话 · 上线诊断报告

> 生成时间：2026-07-21 12:30
> 检查范围：前端 Taro 小程序 + 后端 Node/Express

---

## 🔴 阻塞上线（必须修复，否则无法通过审核/真机运行）

### 1. 前端 BASE_URL 是 localhost（致命）

**文件**：`miniprogram/src/utils/request.ts` 第12行
```typescript
const BASE_URL = 'http://localhost:3000'  // ← 真机无法访问
```

**问题**：
- 真机访问不了 localhost
- 微信强制 HTTPS，HTTP 请求在真机直接被拦截
- 开发者工具模拟器勾了"不校验合法域名"才能跑，真机不行

**修复**：
```typescript
const BASE_URL = 'https://你的已备案域名.com'
```

---

### 2. WECHAT_APP_SECRET 为空（致命）

**文件**：`backend/.env` 第8行
```
WECHAT_APP_SECRET=""
```

**问题**：
- 真机 `wx.login` 的 code 需要后端用 AppID + AppSecret 调微信 `code2Session` 接口兑换 openid
- 没有 Secret，后端只能走 mock 模式返回假 openid，真机登录会失败

**修复**：
1. 登录 [微信公众平台](https://mp.weixin.qq.com) → 开发 → 开发管理 → 开发设置
2. 复制 AppSecret（如果没有就点「重置」生成）
3. 填入 `.env`：
```
WECHAT_APP_SECRET="你的真实AppSecret"
```

---

### 3. AppID 不一致（致命）

| 位置 | AppID |
|------|-------|
| `miniprogram/project.config.json` | `wx268d1063ab9d6f2f` |
| `backend/.env` | `wx231962cec75efb9e` |

**问题**：前后端 AppID 不一致，后端调用微信 API 时会用错误的 AppID，导致登录失败。

**修复**：确认你实际注册的小程序 AppID 是哪个，然后两边统一。到微信公众平台 → 开发设置 查看。

---

### 4. 服务器域名白名单未配置（致命）

**问题**：微信小程序所有网络请求必须在 mp 后台配置合法域名。

**操作步骤**：
1. 登录微信公众平台 → 开发管理 → 开发设置 → 服务器域名
2. **request 合法域名**：添加 `https://你的API域名`
3. **uploadFile 合法域名**：添加 `https://你的API域名`（录音上传需要）
4. **downloadFile 合法域名**：添加 `https://你的API域名`（音频/图片下载需要）
5. 域名必须已备案 + HTTPS + 有效 SSL 证书

---

### 5. 隐私协议未配置（审核必挂）

**问题**：
- 项目使用了 `getRecorderManager`（录音）、`getPhoneNumber`（手机号）等隐私接口
- 微信从 2023-09-15 起强制要求配置隐私协议
- 不配置会被审核驳回

**需要做两件事**：

**(A) mp 后台配置**：
- 设置 → 服务内容声明 → 用户隐私保护指引
- 勾选用到的隐私接口（麦克风、手机号等）
- 填写收集目的

**(B) 代码中处理隐私授权**：
在 `app.tsx` 或首次进入页面添加：
```typescript
// app.tsx
useLaunch(() => {
  if (Taro.requirePrivacyAuthorize) {
    Taro.requirePrivacyAuthorize({
      success: () => console.log('用户同意隐私协议'),
      fail: () => console.log('用户拒绝隐私协议'),
    })
  }
})
```

---

### 6. 管理员弱密码（安全风险）

**文件**：`backend/.env`
```
ADMIN_PASSWORD="admin123"
ADMIN_SALT="bd_dream_admin_salt_v1"
```

**修复**：
```bash
# 生成随机密码和盐值
openssl rand -base64 24  # 作为新密码
openssl rand -hex 16     # 作为新盐值
```

---

## 🟡 建议修复（不阻塞上线但有隐患）

### 7. Gemini 依赖残留

**文件**：
- `backend/server.ts` 第7行：`import { GoogleGenAI, Type } from "@google/genai"`
- `backend/package.json` 第15行：`"@google/genai": "^2.12.0"`

**问题**：项目已切换到 StepFun，但 Gemini 的 import 和依赖还在，增加包体积和潜在安全面。

**修复**：
```bash
cd backend && npm uninstall @google/genai
```
然后删除 server.ts 中所有 Gemini 相关代码（import + 初始化 + 调用）。

---

### 8. 后端无 HTTPS 配置

**问题**：`server.ts` 只监听 HTTP 3000 端口，微信要求 HTTPS。

**修复方案（三选一）**：
- **方案 A**（推荐）：部署到 CloudBase CloudRun，自动 HTTPS
- **方案 B**：Nginx 反向代理 + Let's Encrypt SSL 证书
- **方案 C**：用 `https` 模块直接启 HTTPS（需要 SSL 证书文件）

---

### 9. JSON 文件数据库不适合生产

**文件**：`backend/src/data.json`

**问题**：
- 文件存储，高并发写入有数据丢失风险
- 无事务、无索引、无备份
- 适合开发调试，不适合生产

**修复**：迁移到 CloudBase 云数据库（NoSQL）或 MySQL。

---

### 10. Vite 集成代码残留

**文件**：`backend/server.ts` 末尾 `startServer()` 函数
```typescript
if (process.env.NODE_ENV !== "production") {
  const vite = await createViteServer({ ... });
  app.use(vite.middlewares);
} else {
  // serve dist/
}
```

**问题**：小程序不需要 Vite 前端，这段代码多余且增加启动时间。

**修复**：删除 Vite 集成，直接 `app.listen()`。

---

### 11. 项目名称不一致

| 位置 | 名称 |
|------|------|
| `miniprogram/src/app.config.ts` | `耳畔童话` |
| 项目文档/memory | `伴梦童话` |

**修复**：确认最终名称，统一所有位置。

---

### 12. 缺少 sitemap.json

**问题**：影响微信搜索索引。

**修复**：在 `miniprogram/src/sitemap.json` 添加：
```json
{
  "desc": "关于本文件的更多信息，请参考文档 https://developers.weixin.qq.com/miniprogram/dev/reference/configuration/sitemap.html",
  "rules": [{ "action": "allow", "page": "*" }]
}
```

---

### 13. 无 API 速率限制

**问题**：无限流，可能被恶意刷接口（特别是 story/generate-text 这种调用 StepFun 的端点，每次都有 API 成本）。

**修复**：
```bash
cd backend && npm install express-rate-limit
```
```typescript
import rateLimit from 'express-rate-limit'
const apiLimiter = rateLimit({ windowMs: 60000, max: 30 })
app.use('/api/', apiLimiter)
```

---

## 🟢 可选优化

### 14. tabBar 图标用 SVG 源文件，建议直接用 PNG

当前 `src/assets/tabbar/` 同时有 .svg 和 .png，Taro 编译用 PNG。确认 PNG 尺寸（建议 81x81px）清晰度。

### 15. 加结构化日志

生产环境建议用 `winston` 或 `pino` 替代 `console.log`，方便日志收集和排查。

### 16. 加全局错误处理

后端建议加 `express` 错误处理中间件，统一错误响应格式。

---

## 📋 上线 Checklist

按优先级排序，打勾的做完了就能上线：

- [ ] **前端 BASE_URL 改成 HTTPS 域名**
- [ ] **后端 `.env` 填入 WECHAT_APP_SECRET**
- [ ] **前后端 AppID 统一**
- [ ] **mp 后台配置服务器域名白名单**（request + uploadFile + downloadFile）
- [ ] **mp 后台配置隐私协议**
- [ ] **代码中加隐私授权弹窗处理**
- [ ] **ADMIN_PASSWORD 改强密码**
- [ ] **ADMIN_SALT 改随机值**
- [ ] 后端部署到 HTTPS 环境（CloudBase / Nginx + SSL）
- [ ] 真机测试登录 → 生成故事 → 录音 → 播放全流程
- [ ] 移除 Gemini 依赖
- [ ] 删除 Vite 集成代码
- [ ] 确认项目名称统一
- [ ] 添加 sitemap.json
- [ ] 加 API 速率限制
- [ ] 数据库迁移（生产环境建议）

---

## 🔧 给 Codex 的修改建议

交给 Codex 处理时，按以下优先级：

**P0（必须做）**：1, 2, 3, 4, 5, 6
**P1（强烈建议）**：7, 8, 9, 10
**P2（有空就做）**：11, 12, 13, 14, 15, 16

每个问题的修复代码上面已经给出，Codex 可以直接参考。

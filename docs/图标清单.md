# 伴梦童话 · 图标清单（审计）

> 审计日期：2026-08-05
> 范围：`miniprogram/src` 全部 `*.tsx` + `src/components/Icon` 注册表 + `src/assets/tabbar/*.svg`

## 一、图标体系总览

| 体系 | 位置 | 数量 | 说明 |
|---|---|---|---|
| CSS Vector 自研图标组件 | `src/components/Icon/index.tsx` | **36 个** | 纯 CSS 绘制（border/transform/clip-path/pseudo），无字体依赖，支持 `size`/`color` 动态着色 |
| 遗留 SVG 资源 | `src/assets/tabbar/*.svg` | 6 个 | ⚠️ **全项目零引用**，是 BottomNav 改用 Icon 组件后的死资源 |

> 实际底部导航由 `src/components/BottomNav` 用 Icon 组件渲染（`compass`/`mic`/`user`），**不是** app.json 原生 tabBar。

## 二、Icon 组件注册表（36 个，按用途分类）

### A. 基础形状（8）
`play` · `pause` · `chevron-left` · `chevron-right` · `arrow-right` · `plus` · `check` · `x`

### B. 内容 / 对象（11）
`book-open` · `library` · `folder` · `home` · `star` · `heart` · `heart-filled` · `moon` · `gift` · `award` · `copy`

### C. 操作 / 功能 / 导航（17）
`bell` · `mic` · `volume` · `search` · `edit` · `trash` · `refresh` · `share` · `settings` · `info` · `logout` · `compass` · `user` · `headphones` · `message-circle` · `sparkles` · `clock`

## 三、实际使用位置映射

| 图标 | 使用位置 | 备注 |
|---|---|---|
| `bell` | home（通知铃·橙 `#f97316`）/ my（通知中心）/ notification（空态灰） | |
| `mic` | wizard（克隆入口·紫）/ studio（录音·紫×多）/ **BottomNav**（录音室） | |
| `volume` | story-preview / home（最近播放）/ wizard（配音）/ story-player（×多）/ studio（试听·紫）/ BGM(rain,waves) | |
| `moon` | story-player（睡眠定时×2）/ welcome（登录选项）/ BGM(soft_noise) | |
| `search` | diary（搜索）/ template（搜索） | |
| `edit` | diary（编辑）/ wizard（自定义×3） | |
| `trash` | diary（删除）/ studio（删除·红 `#ef4444`） | |
| `gift` | my（邀请码）/ home（邀请） | |
| `award` | my（VIP 金 `#f59e0b`） | |
| `sparkles` | story-preview（等待态×多·金/紫）/ home（生成+推荐·金）/ wizard（生成按钮·白）/ BGM(wind) | |
| `book-open` | story-preview（×多）/ diary（空态+占位）/ wizard（占位+面板标题）/ home（卡片×多）/ story-player（占位+演示）/ template（占位） | 最高频图标 |
| `compass` | template（空态+场景）/ welcome（登录选项）/ **BottomNav**（首页） | |
| `share` | story-player（分享） | |
| `copy` | my（兑换卡密） | |
| `clock` | profile（成长资料时间·灰） | |
| `user` | my（宝宝资料）/ wizard（主人公）/ welcome（登录选项）/ **BottomNav**（我的） | |
| `info` | story-preview / wizard（提示×3·橙 `#d97706`）/ story-player（教育目标）/ studio（提示·橙）/ template（目标+场景）/ my（关于） | |
| `refresh` | story-preview（重试） | |
| `headphones` | home（最近播放列表）/ template（已听）/ studio（试听·紫） | |
| `star` | story-preview（成果徽章×2·金）/ welcome（登录选项） | |
| `home` | welcome（登录选项，Icon 名，非 tabBar svg） | |
| `library` | SafeImage 兜底默认（`fallbackIcon='library'`） | |
| `logout` | my（退出登录·红 `#dc2626`） | |
| `heart` / `heart-filled` | welcome（登录选项 heart-filled）/ diary（收藏切换）/ story-player（收藏切换） | 动态三态 |
| `message-circle` | welcome（微信登录按钮·白） | |
| `x` | welcome（关闭）/ BGM(关闭 none) | |
| `chevron-left` | NavBar（返回，动态 `a.icon`） | chevron-right 未用 |

## 四、未使用的死图标（7 个，建议清理或保留备用）

`settings` · `folder` · `play` · `pause` · `chevron-right` · `arrow-right` · `plus`

> play/pause 未使用：播放器用的是微信原生 audio 控件 + 自定义按钮，未走 Icon 组件。

## 五、遗留死资源（建议删除）

`src/assets/tabbar/` 下 6 个 SVG 全项目零引用：
`home.svg` · `home-selected.svg` · `studio.svg` · `studio-selected.svg` · `my.svg` · `my-selected.svg`

## 六、⚠️ 主题一致性提示（与当前蓝主题改造相关）

当前蓝主题改造把全局 mixin 转蓝，但大量图标仍**硬编码紫色 `#8b5cf6`**，与蓝主题冲突，集中在录音/创作链路：

- `studio/index.tsx`：`mic` ×4、`headphones` ×1、`volume` ×1 均为 `#8b5cf6`
- `wizard/index.tsx`：`mic` ×1 为 `#8b5cf6`
- `home/index.tsx`：`book-open` 一处为 `#4f73e6`（已接近蓝，但非 token）、`sparkles` 为金 `#f59e0b`
- `story-preview/index.tsx`：`book-open` 一处 `#8b5cf6`、`sparkles` 金 `#f59e0b`

建议：把 `#8b5cf6` 这些硬编码统一替换为 `$primary-500`（或对应 token），让录音室/向导也跟随蓝主题。
